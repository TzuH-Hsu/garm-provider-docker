runner_image = "ghcr.io/example/garm-runner-noble@sha256:deadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
dind_mode = "privileged-sidecar"
dind_image = "docker:dind"
allow_unpinned_dind_image = true
# allowed_dind_modes must be widened explicitly: the ceiling now
# defaults to ["none"] (fail-closed), and this fixture targets a
# dind_image branch, not the ceiling check.
allowed_dind_modes = ["none", "privileged-sidecar"]
