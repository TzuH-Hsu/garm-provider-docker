# M4-W1: builds and publishes this repo's two container images to GHCR.
#
# Two structurally separate jobs, never both selected for one run:
#
#   publish — the ONLY job that can reach GHCR. Gated on
#     `github.event_name == 'push' && github.ref_type == 'tag'`, so it can
#     only ever run for a genuinely pushed `v*` tag, and it is the only job
#     carrying `packages: write`.
#   dry-run — everything else (i.e. `workflow_dispatch`). Runs the exact
#     same multi-arch build with `push: false`, holds NO `packages` scope at
#     all, and has no registry-login step to skip in the first place.
#
# The split is deliberate and load-bearing: `workflow_dispatch` accepts a
# `--ref` of any kind, INCLUDING a tag (`gh workflow run --ref v1.2.3`), in
# which case `github.ref_type` is `tag` even though nothing was pushed. A
# single job that keyed its login/push decision on `ref_type` alone would
# therefore publish semver+latest from a manual invocation — breaking the
# dry-run promise. Requiring `event_name == 'push'` as well, and scoping
# `packages: write` to that job only, means there is no path by which a
# manual run can authenticate to or write to the registry: even if the
# `push` input were somehow forced true, the dispatch job's token has no
# packages scope to push with.
#
# No `pull_request` trigger: this workflow only ever runs against a ref that
# has already been reviewed and merged (a pushed tag) or an explicit manual
# invocation.
#
# Script-injection hygiene (same rationale as release.yml): git tag names
# are not restricted to a safe shell-metacharacter-free set, so
# $GITHUB_REF_NAME (GitHub's own built-in env var) is read as a quoted shell
# variable inside `run:` steps below, never spliced via `${{ }}` into script
# text. Values that end up in `with:` inputs to the pinned Docker actions
# (image tags, build-args) are not run through a shell at all, and Docker's
# own tag-name validation rejects anything a malicious ref could smuggle in
# there regardless.
name: Container Images

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: images-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

# Workflow-level default is read-only. `packages: write` is granted ONLY to
# the publish job below; the dry-run job explicitly holds none.
permissions:
  contents: read

jobs:
  # Real publish: a pushed vX.Y.Z tag, and nothing else.
  publish:
    name: publish (${{ matrix.name }})
    if: github.event_name == 'push' && github.ref_type == 'tag'
    # Stays on ubuntu-latest: multi-platform buildx/QEMU builds need a real
    # Docker daemon, which ubuntu-slim's container-only runtime cannot
    # provide (see ci.yml's dockerverify job comment for the same
    # constraint, and actions/runner-images#13541).
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # (a) the runner image (ADR-002): built from its own directory so
          # entrypoint.sh's relative COPY resolves the same way it does for
          # a local `docker build runner-images/noble`.
          - name: garm-runner-noble
            image: ghcr.io/tzuh-hsu/garm-runner-noble
            context: runner-images/noble
            dockerfile: runner-images/noble/Dockerfile
            needs_version_arg: false
          # (b) the provider binary image (plan.md M4 item 1): built from
          # the repo root so the root Dockerfile's `COPY . .` sees the
          # module.
          - name: garm-provider-docker
            image: ghcr.io/tzuh-hsu/garm-provider-docker
            context: .
            dockerfile: Dockerfile
            needs_version_arg: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up QEMU
        uses: docker/setup-qemu-action@99012661954931238ded8c8b007157a8430204e1 # v4.4.0

      - name: Set up Buildx
        uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1

      # A pushed vX.Y.Z tag IS the version — this job's `if:` guarantees
      # $GITHUB_REF_NAME is that tag, so there is no branching left to do.
      # "tags" is written via the GITHUB_OUTPUT heredoc form to carry a real
      # newline (this repo's convention for multi-line step outputs) rather
      # than being reconstructed later with `${{ }}` splicing.
      - name: Determine build metadata
        id: meta
        env:
          IMAGE: ${{ matrix.image }}
        run: |
          version="${GITHUB_REF_NAME}"
          # printf, not a quoted multi-line string literal, so no YAML
          # indentation whitespace ends up baked into the second line.
          tags="$(printf '%s:%s\n%s:latest' "${IMAGE}" "${version}" "${IMAGE}")"
          {
            echo "version=${version}"
            echo "tags<<EOF"
            echo "${tags}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # provenance: mode=max / sbom: true match the dry-run job exactly
      # (verified locally: buildx generates both for a multi-platform build
      # even with push: false, so the dry run exercises the identical
      # attestation path a real release does).
      #
      # RULING (M4-W1, not to be revisited here): per-platform attestations
      # are accepted as-is; no cosign keyless signing is added. Known
      # quirk: for a multi-arch manifest LIST, provenance/SBOM attestations
      # are attached per-platform-manifest, not once for the top-level
      # index — a client that inspects the manifest list itself (rather
      # than resolving to one platform's digest first, or using the OCI
      # Referrers API) will not see a single combined attestation, and GHCR's
      # Referrers API support for discovering them by digest should be
      # verified against whatever tooling operators point at this image.
      - name: Build and push
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          build-args: ${{ matrix.needs_version_arg && format('VERSION={0}', steps.meta.outputs.version) || '' }}
          provenance: mode=max
          sbom: true

  # Dry run: every trigger that is NOT a pushed tag — in practice
  # workflow_dispatch, including a dispatch aimed at a tag ref. Builds only.
  # It holds no `packages` scope, performs no registry login, and passes
  # `push: false`, so there is no path from here to GHCR.
  dry-run:
    name: dry-run (${{ matrix.name }})
    if: github.event_name != 'push' || github.ref_type != 'tag'
    # Stays on ubuntu-latest: same buildx/QEMU Docker requirement as the
    # publish job above; ubuntu-slim cannot run a Docker daemon.
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      packages: none
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: garm-runner-noble
            image: ghcr.io/tzuh-hsu/garm-runner-noble
            context: runner-images/noble
            dockerfile: runner-images/noble/Dockerfile
            needs_version_arg: false
          - name: garm-provider-docker
            image: ghcr.io/tzuh-hsu/garm-provider-docker
            context: .
            dockerfile: Dockerfile
            needs_version_arg: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up QEMU
        uses: docker/setup-qemu-action@99012661954931238ded8c8b007157a8430204e1 # v4.4.0

      - name: Set up Buildx
        uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1

      # A synthetic version that can never collide with a real release tag,
      # while still exercising the identical build-arg/-ldflags path. The
      # ref name is deliberately NOT consulted here: a dispatch may point at
      # a tag, and treating that as a release version is exactly the
      # confusion this job's separation exists to prevent.
      - name: Determine build metadata
        id: meta
        env:
          IMAGE: ${{ matrix.image }}
        run: |
          version="v0.0.0-dryrun-${GITHUB_SHA::12}"
          {
            echo "version=${version}"
            echo "tags=${IMAGE}:${version}"
          } >> "$GITHUB_OUTPUT"

      - name: Build (no push)
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          build-args: ${{ matrix.needs_version_arg && format('VERSION={0}', steps.meta.outputs.version) || '' }}
          provenance: mode=max
          sbom: true

      - name: Dry-run summary
        env:
          VERSION: ${{ steps.meta.outputs.version }}
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          {
            echo "### Container image dry run"
            echo
            echo "No registry login was performed and nothing was pushed."
            echo "This job holds no \`packages\` permission."
            echo
            echo "- Synthetic version: \`${VERSION}\`"
            echo "- Tag that WOULD have been built: \`${TAGS}\`"
            echo "- A real release publishes \`<image>:<tag>\` plus \`<image>:latest\`."
          } >> "$GITHUB_STEP_SUMMARY"
